import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  mapView: {
    width: '100%',
    height: '100%',
  },
  listContainer: {
    position: 'absolute',
    bottom: 10,
  },
});

export default styles;
